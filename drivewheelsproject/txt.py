# response = redirect('/booking/' + car_id)
        # response.set_cookie('rentValue', totalRent)
        # print(request.cookies.get('rentValue'))

        #query_params = urlencode({'total': totalRent})
        #print(query_params)


        #cb=carbooking.objects.create(datetime=dt,hours=hr)
        #cb.save()

        #return redirect(f'/booking/{car_id}{query_params}')